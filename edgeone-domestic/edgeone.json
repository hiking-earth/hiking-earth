{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "static",
  "mainlandRegions": ["ap-guangzhou"],
  "cloudFunctions": {
    "nodejs": {
      "includeFiles": ["runtime/**", "static/**"],
      "maxDuration": 120
    }
  }
}
