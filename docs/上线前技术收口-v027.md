# 徒步地球上线前技术收口 v027

日期：2026-09-01

## 本轮技术收口

- [x] 建立 `v027-上线前技术收口前-2026-09-01` 修改前回退档。
- [x] 将 `gibs.earthdata.nasa.gov` 加入生产 CSP 的 `img-src`。
- [x] 将 `gibs.earthdata.nasa.gov` 加入生产 CSP 的 `connect-src`。
- [x] 增加 NASA GIBS CSP 自动回归断言。
- [x] 移除会在中文项目路径下生成非法 `Link` 响应头的 `next/font` 构建产物，改用跨平台系统中文字体栈。
- [x] 增加生产字体响应头回归断言。
- [x] 同步 README、版本说明和 v026 真机验收记录。
- [x] 完成 lint、完整构建和 13/13 自动测试。
- [x] 完成本地生产 HTTP 回归：首页、法律/来源/发布页面、健康检查和 robots 均返回 200，安全响应头生效。
- [x] 完成浏览器可视化回归：生产首页标题、主标题和 20 条路线卡片均正常显示。
- [x] 整理并提交可追溯的 Git 候选基线。

## 域名与发布边界

- `.com` 域名在候选版测试通过后购买和绑定。
- 正式域名确定后填写 `NEXT_PUBLIC_SITE_URL=https://你的域名`。
- 数据许可、正式联系信息和剩余真机验收未完成前，`NEXT_PUBLIC_RELEASE_READY` 必须保持 `false`。
- 域名购买和 HTTPS 可先完成，但“域名可访问”不等于“允许正式对外发布”。

## 仍阻断正式发布

- 20 条路线的官方开放状态、核验日期、轨迹来源、长度和爬升信息未全部复核。
- SmartWeatherAPI 正式凭据、缓存与限频策略未完成。
- Unsplash 逐图权利台账或自有图片替换未完成。
- Esri World Imagery 生产账号、配额、计费和使用方式未确认。
- 正式联系邮箱、隐私负责人、投诉举报方式和数据保存期限未填写。
- iPhone HTTPS 定位、Android Chrome、Windows Chrome/Edge 验收未完成。

## 正式上线顺序

1. 完成本文件的技术验证和 Git 候选基线。
2. 购买并配置 `.com` 域名、部署平台和 HTTPS。
3. 完成真实数据、图片、地图和天气许可。
4. 补齐法律联系信息并完成三端真实设备验收。
5. 建立最终上线前不可变留档。
6. 最后才将 `NEXT_PUBLIC_RELEASE_READY` 改为 `true`，重新构建并部署。
